{
  "name": "assignment2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "frontend": "npm start --prefix frontend ",
    "backend": "npm start --prefix backend",
    "start": "concurrently \"npm run frontend \" \"npm run backend\"",
    "inst-all": "npm install && npm install --prefix Frontend && npm install --prefix Backend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
